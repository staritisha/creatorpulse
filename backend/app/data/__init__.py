"""
data — static data directory (mock JSON files, disk cache).

This package contains no importable Python code.
Mock data lives in data/mock/*.json and is accessed via
config.constants.MOCK_*_PATH or services.cache_service.cache.get_mock().
"""
