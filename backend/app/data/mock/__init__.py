"""
data/mock — demo-mode JSON datasets.

Files here are loaded by services.cache_service.CacheService.get_mock()
and are not importable Python modules.

    youtube_mock.json   ~30 realistic YouTube videos with analytics
    discord_mock.json   ~50 community messages with sentiment + topics
    sheets_mock.json    creator engagement log rows
    insights_mock.json  pre-generated AI insight cards + recommendations
    resonance_mock.json per-video resonance scores + topic summaries
"""
