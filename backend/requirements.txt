# CreatorPulse backend dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# AI / LLM
anthropic>=0.30.0

# HTTP client (for YouTube, Discord service calls)
httpx>=0.27.0

# Environment variables
python-dotenv>=1.0.0

# In-memory caching (no Redis needed)
cachetools>=5.3.0

# Numerical scoring (resonance, growth predictor)
numpy>=1.26.0

# Google Sheets service
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0
